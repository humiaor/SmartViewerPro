# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

targetsArray = ['SmartViewerPro']

targetsArray.each do |t|
    target t do
  pod 'Masonry'
  pod 'FFmpeg'
    end
end

